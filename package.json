{
  "name": "graphql-node-minimum-exercise",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "prettier": "prettier --write src/*",
    "server": "nodemon src/server.js",
    "browser-sync": "browser-sync start --proxy 'localhost:4000' --files 'src/*.css' 'src/*.html'"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-graphql": "^0.9.0",
    "graphql": "^14.6.0"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.1",
    "eslint-plugin-prettier": "^3.1.2",
    "nodemon": "^2.0.3",
    "prettier": "^1.19.1"
  }
}
